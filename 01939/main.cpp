#include <cstdio>
using namespace std;
int n, m, start, end, weight;
int main() {
    scanf("%d %d", &n, &m);
    for(int i=0;i<m;++i){
        scanf("%d %d %d", &start, &end, &weight);
    }
    return 0;
}
